<x-service-desk-admin-layout title="Assigned Requests" subtitle="Requests currently assigned to you. Resolve them or return them to the shared support queue." section="assigned">
    <div class="rounded-[1.875rem] bg-white p-8 text-slate-950">
        <div class="mb-6 flex items-center justify-between gap-4"><h2 class="text-2xl font-bold">My active work</h2><span class="rounded-full bg-blue-50 px-4 py-2 text-sm font-bold text-[#346DCB]">{{ $tickets->count() }} assigned</span></div>
        <div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead><tr class="border-b-2 border-slate-200 uppercase tracking-wide text-slate-500"><th class="py-3">Ticket</th><th class="py-3">Client</th><th class="py-3">Subject</th><th class="py-3">Priority</th><th class="py-3">Status</th><th class="py-3 text-right">Action</th></tr></thead><tbody>@forelse ($tickets as $ticket)<tr class="border-b border-slate-200"><td class="py-4 font-semibold">{{ $ticket->ticket_no }}</td><td class="py-4">{{ $ticket->client_name ?? 'Internal' }}</td><td class="py-4">{{ $ticket->subject }}</td><td class="py-4">{{ $ticket->priority }}</td><td class="py-4">{{ $ticket->status }}</td><td class="py-4 text-right"><form method="POST" action="{{ route('admin.itsm.service-desk.release', $ticket) }}" class="inline">@csrf @method('PATCH')<button class="rounded-md border border-slate-300 px-3 py-1 font-semibold hover:bg-slate-100">Release</button></form></td></tr>@empty<tr><td colspan="6" class="py-12 text-center text-slate-500">No requests are assigned to you.</td></tr>@endforelse</tbody></table></div>
    </div>
</x-service-desk-admin-layout>
